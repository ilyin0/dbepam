SET TIME ON
SET TIMING ON

DEFINE USER_NAME = &&1

SPOOL DROP_SCHEMA.LOG

SET SERVEROUTPUT ON

PROMPT Name of user to be dropped: &&USER_NAME
PROMPT

@drop_user.sql &&USER_NAME

PROMPT Name of tablespace to be dropped: '&&USER_NAME._DATA'
PROMPT

@drop_tablespace.sql &&USER_NAME

SET SERVEROUTPUT OFF

UNDEFINE USER_NAME

SPOOL OFF